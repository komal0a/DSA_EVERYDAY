
2029. Stone Game IX

class Solution {
public: 
    bool stoneGameIX(vector<int>& stones) {
   int cnt0 = 0;
        int cnt1 = 0;
        int cnt2 = 0;

        for (int x : stones) {
            if (x % 3 == 0)
                cnt0++;
            else if (x % 3 == 1)
                cnt1++;
            else
                cnt2++;
        }

        // Even number of multiples of 3
        if (cnt0 % 2 == 0) {
            return cnt1 > 0 && cnt2 > 0;
        }

        // Odd number of multiples of 3
        return abs(cnt1 - cnt2) >= 3;
    }
};
